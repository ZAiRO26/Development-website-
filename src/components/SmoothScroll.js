import React from 'react';

// Native Smooth Scroll - Zero bloat, maximum performance
// We rely on CSS 'scroll-behavior: smooth' in index.css
const SmoothScroll = ({ children }) => {
    return <>{children}</>;
};

export default SmoothScroll;
